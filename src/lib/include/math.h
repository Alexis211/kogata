#pragma once

#include "fdlibm.h"

#define HUGE_VAL ((double)HUGE)
#define HUGE_VALF ((float)HUGE)
#define HUGE_VALL ((long double)HUGE)

double log2(double x);


/* vim: set sts=0 ts=4 sw=4 tw=0 noet :*/
